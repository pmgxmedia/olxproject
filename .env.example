# Frontend API base URL
# Development: point directly at the local backend
VITE_API_URL=http://localhost:8000
# Production on Sevalla: the frontend is served by the same FastAPI app,
# so relative /api calls work automatically — no need to set this variable.
